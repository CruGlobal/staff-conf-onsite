<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : t('checkin.title') %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body class="checkin-page">
  <div class="container">
    <h1><%= content_for?(:page_title) ? yield(:page_title) : t('checkin.page_title') %></h1>
    
    <div class="user-info">
      <%= t('checkin.welcome', name: current_user.full_name) %> | 
      <%= link_to t('checkin.dashboard'), root_path %>
    </div>

    <%= yield %>

    <%= javascript_include_tag 'application' %>
    <%= yield :javascript %>
  </div>
</body>
</html> 